import React from 'react';

const ProgressTracker = ({ userId }) => (
    <div className="progress-tracker">
        <h3>Progress Tracker</h3>
        {/* Progress chart placeholder */}
        <div>Progress chart for user {userId}</div>
    </div>
);

export default ProgressTracker;
