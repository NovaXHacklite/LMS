import React from 'react';

const QuizComponent = ({ userId }) => (
    <div className="quiz-component">
        <h3>Adaptive Quiz</h3>
        {/* Quiz UI placeholder */}
        <div>Quiz will appear here for user {userId}</div>
    </div>
);

export default QuizComponent;
