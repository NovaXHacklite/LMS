import React from 'react';

const ChatbotInterface = ({ userId }) => (
    <div className="chatbot-interface">
        <h3>AI Chatbot</h3>
        {/* Chatbot UI placeholder */}
        <div>Chatbot for user {userId}</div>
    </div>
);

export default ChatbotInterface;
